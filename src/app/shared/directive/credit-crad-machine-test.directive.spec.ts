import { CreditCradMachineTestDirective } from './credit-crad-machine-test.directive';

describe('CreditCradMachineTestDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditCradMachineTestDirective();
    expect(directive).toBeTruthy();
  });
});
